#include <bits/stdc++.h>

#ifndef lol
#include "debug.h"
#else
#define debug(...) 42
#define debug_assert(...) 42
#endif

using namespace std;

int
main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int T;
    cin >> T;
    while (T--) {
    }
    return 0;
}
