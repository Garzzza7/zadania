#include <iostream>
#include <vector>

template <typename T = int> struct hungarian {};

int
main(void) {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    return 0;
}
