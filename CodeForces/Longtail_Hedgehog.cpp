#include <bits/stdc++.h>
#include <cmath>
#include <stdlib.h>
using namespace std;
//https://codeforces.com/problemset/problem/615/B
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    long long r=0;
    vector<int> v;
    for(inti = 0 ; i <= n-1 ; i++){
        r+=v[i];
    } 
    return 0;
}








