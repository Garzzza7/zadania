#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int T;
    cin >> T;
    while (T--) {
	int n, m;
	cin >> n >> m;
	string s;
	vector<string> vec(n);
	for (int i = 0; i < n; i++) {
	    string ss;
	    cin >> ss;
	    vec[i] = ss;
	}
	while (n * m != 1) {
	}
    }
    return 0;
}
