#include <cmath>
#include <iostream>
#include <string>
#include <vector>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int T;
    std::cin >> T;
    while (T--) {
	long long a;
	std::cin >> a;
	std::string res = "";
	std::string x = std::to_string(a);
	for (int i = 0; i < (int) x.size(); i++) {
	    int num;
	}
    }
    return 0;
}
