#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int T;
    cin >> T;
    while (T--) {
	int n;
	cin >> n;
	vector<long long> vec(n);
	int cntm = 0;
	int cntn for (int i = 0; i < n; i++) {
	    long long aa;
	    cin >> aa;
	    vec[i] = aa;
	}
    }
    return 0;
}
