#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <map>
#include <vector>
#include <math.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin>>t;
    while(t--){
        int al;
        cin>>al;
        vector<int> a;
        vector<int> b;
        vector<int> result;
        for(int i = 0 ; i < al ; i++){
            int element;
            cin>>element;
            a.push_back(element);
            b.push_back(0);
            if(i>=2){
                for(){

                }
            }
        }
        for(int i = 0 ; i < a.size() ; i++){

        }


    }
    return 0;
}
