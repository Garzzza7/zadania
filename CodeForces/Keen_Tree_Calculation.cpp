#include <bits/stdc++.h>
#include <cmath>
#include <stdlib.h>
using namespace std;
struct Node
{
public:
    Node* left;
    Node* right;
    int value=0;
    long long sum=0;
};
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    vector<Node> vec;
    for(int i = 0 ; i < n ; i++){
        
    }

    return 0;
}
